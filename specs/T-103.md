# T-103: Konfigurationsmodell festlegen (Azure-JSON fix, Sprache/Device persistent)

## Kontext
Status: review
Priorität: medium
Parent Task: T-100
Abhängigkeiten: T-100

## Goal
Konfigurationsmodell für Azure + User-Einstellungen (Sprache/Devices) verbindlich festlegen.

## Done When
- [x] Feste Azure-JSON-Struktur ist beschrieben.
- [x] Persistenzformat für User-Settings steht fest.
- [x] Default-/Fallback-Regeln für Sprache und Devices dokumentiert.
- [x] Optionaler Proxy-Block (`proxy.host`, `proxy.port`, optional Auth) ist im Contract validiert und dokumentiert.

## Approach
- Systemweite Defaults laden, User-Settings darüber mergen.
- Konfigurationsvalidierung beim Start durchführen.
- Fehlerhafte Konfiguration mit klarer UI-Meldung behandeln.

## Log
- 2026-06-03: Spec detailliert ausgefüllt.
- 2026-06-06: Config-Contract (`src/shared/config-contract.ts`) ergänzt, Beispiel-JSONs für Fixed Azure Config und User-Settings angelegt (`config/*.example.json`) und Regeln/Fallbacks in README dokumentiert.
- 2026-06-18: Fixed-Azure-Contract erweitert um optionalen Proxy (`AzureProxyConfig`), `recognitionMode` aus Contract/Beispielkonfiguration entfernt und Validierung entsprechend angepasst.
